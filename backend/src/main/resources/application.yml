server:
  port: 8081

spring:
  application:
    name: nuclear-energy
  profiles:
    active: dev

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:/mapper/*.xml

logging:
  level:
    org.example.nuclearenergypopularization: debug